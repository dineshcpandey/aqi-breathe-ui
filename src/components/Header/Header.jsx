import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <div className="header">
            <h1>🌍 AQI Mapping Platform</h1>
            <p>Interactive map with configurable layers, ready for Air Quality Index data visualization</p>
        </div>
    );
};

export default Header;